# Simple VidCompress (Refactoring in Progress 🏗️)

This project is currently undergoing a major architectural refactor. 
The previous documentation is outdated and has been removed.

### Current Status:
- [x] Core logic refactor
- [x] Misc functions
- [ ] Hardcode -> Arguments
- [ ] New documentation

### Quick Start (New Version):
To run the current build, main.py with pipeline function is entry point, but multiple parameter is being hardcoded so they need to be manually modified:
`cd app && python main.py`

To test current build
`PYTHONPATH=. pytest`